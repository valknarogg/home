# ===================================================================
# KOMPOSE - Local Development Domain Configuration
# ===================================================================
# This file is for LOCAL DEVELOPMENT ONLY
# Use localhost with different ports instead of domain names
# ===================================================================

# -------------------------------------------------------------------
# Root Domain (Local)
# -------------------------------------------------------------------
# For local development, we use localhost
ROOT_DOMAIN=localhost

# -------------------------------------------------------------------
# Subdomain Configuration (Local Ports)
# -------------------------------------------------------------------
# For local development, we'll use port numbers instead of subdomains
# These will be accessible as http://localhost:PORT

# Core Infrastructure
SUBDOMAIN_PROXY=localhost:8080
SUBDOMAIN_AUTH=localhost:8180

# Development & Automation
SUBDOMAIN_CODE=localhost:3001         # Gitea
SUBDOMAIN_CHAIN=localhost:5678        # n8n
SUBDOMAIN_AUTO=localhost:3000         # Semaphore

# Content & Communication
SUBDOMAIN_DOCS=localhost:3003
SUBDOMAIN_CHAT=localhost:8085         # Gotify
SUBDOMAIN_MAIL=localhost:8025         # Mailhog

# Data & Analytics
SUBDOMAIN_TRACK=localhost:3007        # Umami

# Applications
SUBDOMAIN_HOME=localhost:8123         # Home Assistant
SUBDOMAIN_VAULT=localhost:8081        # Vaultwarden
SUBDOMAIN_LINK=localhost:3009
SUBDOMAIN_MANAGE=localhost:3100       # KMPS

# Utilities
SUBDOMAIN_VPN=localhost:51821
SUBDOMAIN_OAUTH2=localhost:4180       # OAuth2 Proxy
SUBDOMAIN_ZIGBEE=localhost:8080       # Zigbee2MQTT

# -------------------------------------------------------------------
# Base Configuration
# -------------------------------------------------------------------
BASE_DOMAIN=localhost

# -------------------------------------------------------------------
# SSL/TLS Configuration (Disabled for Local)
# -------------------------------------------------------------------
# No SSL needed for local development
ACME_EMAIL=dev@localhost

# -------------------------------------------------------------------
# Local Development Notes
# -------------------------------------------------------------------
# 1. No Traefik routing - direct port access
# 2. No SSL certificates needed
# 3. Services accessible at http://localhost:PORT
# 4. Database connections use localhost
# 5. No OAuth2 Proxy needed (can disable SSO)
#
# To use this configuration:
#   ./kompose.sh setup local
#   OR
#   cp domain.env.local domain.env
#   cp .env.local .env
#   ./kompose.sh up <stack>
# ===================================================================
