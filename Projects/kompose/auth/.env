# =================================================================
# AUTH Stack Configuration
# =================================================================

# Stack identification
COMPOSE_PROJECT_NAME=auth

# Docker images
DOCKER_IMAGE=quay.io/keycloak/keycloak:latest

# Database name
DB_NAME=keycloak

# Traefik configuration
TRAEFIK_ENABLED=true
TRAEFIK_HOST=${TRAEFIK_HOST_AUTH}

# Keycloak Admin
KC_ADMIN_USERNAME=admin

# =================================================================
# OAuth2 Proxy Configuration (SSO)
# =================================================================

# OAuth2 Proxy hostname
OAUTH2_PROXY_HOST=${TRAEFIK_HOST_OAUTH2:-sso.${BASE_DOMAIN}}

# OAuth2 Client Configuration (must match Keycloak client)
OAUTH2_CLIENT_ID=kompose-sso

# Base domain for cookie sharing across subdomains
BASE_DOMAIN=${BASE_DOMAIN}

# NOTE: Secrets are stored in root secrets.env file
# Required secrets for this stack:
#   - AUTH_KC_ADMIN_PASSWORD=CHANGE_ME_GENERATE_WITH_KOMPOSE
#   - AUTH_OAUTH2_CLIENT_SECRET=CHANGE_ME_GENERATE_WITH_KOMPOSE
#   - AUTH_OAUTH2_COOKIE_SECRET=CHANGE_ME_32_BYTE_BASE64 (generate with: openssl rand -base64 32)
