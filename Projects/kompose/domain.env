# ===================================================================
# KOMPOSE - Domain Configuration
# ===================================================================
# This file contains domain-specific configuration
# Change ROOT_DOMAIN to customize all service subdomains for your setup
# All services will automatically use <subdomain>.${ROOT_DOMAIN}
# ===================================================================

# -------------------------------------------------------------------
# Root Domain
# -------------------------------------------------------------------
# This is the primary domain for your Kompose installation
# All services will be available as subdomains of this domain
# Example: If ROOT_DOMAIN=pivoine.art
#   - proxy.example.com
#   - auth.example.com
#   - etc.
ROOT_DOMAIN=pivoine.art

# -------------------------------------------------------------------
# Subdomain Configuration
# -------------------------------------------------------------------
# Customize individual service subdomains here
# Format: SUBDOMAIN_<SERVICE>=subdomain
# The full URL will be: ${SUBDOMAIN_<SERVICE>}.${ROOT_DOMAIN}

# Core Infrastructure
SUBDOMAIN_PROXY=proxy
SUBDOMAIN_AUTH=auth

# Development & Automation
SUBDOMAIN_CODE=code          # Gitea - Git repository
SUBDOMAIN_CHAIN=chain        # n8n - Workflow automation
SUBDOMAIN_AUTO=auto          # Semaphore - Ansible automation

# Content & Communication
SUBDOMAIN_BLOG=blog          # Blog platform
SUBDOMAIN_DOCS=docs          # Documentation site
SUBDOMAIN_NEWS=news          # Newsletter platform
SUBDOMAIN_CHAT=chat          # Gotify - Notifications
SUBDOMAIN_MAIL=mail          # Mailhog - Email testing

# Data & Analytics
SUBDOMAIN_DATA=data          # Data warehouse
SUBDOMAIN_DASH=dash          # Dashboard
SUBDOMAIN_TRACK=umami        # Umami - Analytics
SUBDOMAIN_TRACE=trace        # Tracing/monitoring

# Applications
SUBDOMAIN_HOME=home          # Home Assistant
SUBDOMAIN_SEXY=sexy          # Directus - Headless CMS
SUBDOMAIN_VAULT=vault        # Vaultwarden - Password manager
SUBDOMAIN_LINK=link          # Link shortener
SUBDOMAIN_DOCK=dock          # Docker registry

# Utilities
SUBDOMAIN_VPN=vpn            # WireGuard VPN
SUBDOMAIN_OAUTH2=sso         # OAuth2 Proxy - SSO

# -------------------------------------------------------------------
# Advanced Configuration
# -------------------------------------------------------------------
# Base domain for cookie sharing (used by OAuth2 Proxy)
# This allows SSO cookies to work across all subdomains
BASE_DOMAIN=${ROOT_DOMAIN}

# Alternative: If you want cookies to work across multiple domains
# BASE_DOMAIN=example.com

# -------------------------------------------------------------------
# SSL/TLS Configuration
# -------------------------------------------------------------------
# Let's Encrypt email for SSL certificates
# This is used by Traefik for automatic SSL certificate generation
ACME_EMAIL=valknar@${ROOT_DOMAIN}

# -------------------------------------------------------------------
# Usage Notes
# -------------------------------------------------------------------
# 1. To change your domain for all services:
#    - Edit ROOT_DOMAIN above
#    - Restart services: ./kompose.sh down && ./kompose.sh up
#
# 2. To customize individual subdomains:
#    - Edit the SUBDOMAIN_* variables above
#    - The full hostname will be: subdomain.ROOT_DOMAIN
#
# 3. DNS Configuration Required:
#    - Create A records for each subdomain pointing to your server
#    - Or use wildcard DNS: *.${ROOT_DOMAIN} -> your-server-ip
#
# 4. First-time setup:
#    - Ensure DNS is configured
#    - Run: ./kompose.sh up proxy
#    - Wait for SSL certificates to be issued
#    - Then start other services
#
# ===================================================================
