# =================================================================
# KMPS (Kompose Management Portal & SSO) Configuration
# =================================================================

# Stack identification
COMPOSE_PROJECT_NAME=kmps

# Docker image (Node.js for Next.js app)
DOCKER_IMAGE=node:20-alpine

# Database name
DB_NAME=kmps

# App Port
APP_PORT=3100

# Traefik configuration
TRAEFIK_ENABLED=true
TRAEFIK_HOST=${TRAEFIK_HOST_KMPS:-manage.${BASE_DOMAIN}}

# Keycloak Client Configuration
KMPS_CLIENT_ID=kmps-admin

# Environment
NODE_ENV=production

# NOTE: Secrets are stored in root secrets.env file
# Required secrets for this stack:
#   - KMPS_CLIENT_SECRET=CHANGE_ME_GENERATE_WITH_KOMPOSE
#   - KMPS_NEXTAUTH_SECRET=CHANGE_ME_GENERATE_WITH_KOMPOSE (generate with: openssl rand -base64 32)
